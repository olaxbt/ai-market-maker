"""Backtest entrypoints (thin wrappers around `src/backtest`).

Run `python -m cli_backtest.driver` after `uv sync` (editable install); the driver
also prepends `src/` to `sys.path` if needed for one-off runs without installing.
"""
